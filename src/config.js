export const firstFieldValues = [...Array(19).keys()].map(i => i + 1);
export const secondFieldValues = [...Array(2).keys()].map(i => i + 1);

export const firstFieldMaxSize = 8;
export const secondFieldMaxSize = 1